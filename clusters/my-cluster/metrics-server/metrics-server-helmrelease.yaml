apiVersion: helm.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metrics-server
  namespace: metrics-server
spec:
  interval: 5m
  chart:
    spec:
      chart: metrics-server
      version: '>=5.0.0 <6.0.0'
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: metrics-server
      interval: 1m
